package constants

// Environment constants
const (
	// Development : Dev env
	Development = "dev"

	// Production : Production env
	Production = "prod"

	// Test : Test env
	Test = "test"
)

const (
	// RequestID: holds the unique request identifier for the request
	RequestID = "request_id"

	// Mode : cli flag to specify env
	Mode = "mode"

	// Request: keys where request data is available
	Request = "request"
)

const (
	Env = "env"

	BasePath = "base_path"

	DefaultBasePath = "."

	Command = "command"
)

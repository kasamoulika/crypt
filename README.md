# crypt
A high-performance Go app for real-time stock data

- Uses Go Modules
- Uses Docker's multi-stage builds

### Running Locally
Build Application:
$ go build -o crypt

Run Application:
$ ./crypt

Run Tests:
$ go test -v ./...
